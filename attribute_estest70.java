package org.jsoup.nodes;

import org.junit.Test;
import static org.junit.Assert.assertTrue;

/**
 * This test class contains an improved version of a test originally generated by EvoSuite.
 * The focus of the improvements is on clarity, maintainability, and expressing intent.
 */
public class Attribute_ESTestTest70 { // The class name is kept for context.

    /**
     * Verifies that hasDeclaredValue() returns true for an attribute initialized with a non-null value.
     *
     * The hasDeclaredValue() method is designed to check if a value has been explicitly set
     * for an attribute, which is true for any non-null value (including an empty string).
     */
    @Test
    public void hasDeclaredValueShouldReturnTrueWhenValueIsSet() {
        // Arrange: Create an attribute with a key and a non-null value.
        Attribute attribute = new Attribute("id", "test-id");

        // Act: Call the method under test.
        boolean hasValue = attribute.hasDeclaredValue();

        // Assert: The result should be true, confirming a value was declared.
        assertTrue("An attribute initialized with a non-null value should be considered as having a declared value.", hasValue);
    }
}