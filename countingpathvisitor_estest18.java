package org.apache.commons.io.file;

import java.nio.file.Path;
import java.nio.file.attribute.BasicFileAttributes;
import org.evosuite.runtime.mock.java.io.MockFile;
import org.junit.Test;

/**
 * This test class was improved for understandability. The original was generated by EvoSuite.
 * A more conventional name for this class would be CountingPathVisitorTest.
 */
public class CountingPathVisitor_ESTestTest18 {

    /**
     * Tests that visitFile() throws a NullPointerException when the BasicFileAttributes argument is null.
     */
    @Test(expected = NullPointerException.class)
    public void visitFileShouldThrowNullPointerExceptionWhenAttributesAreNull() {
        // Arrange: Create a visitor and a dummy path. The attributes are explicitly null.
        final CountingPathVisitor visitor = CountingPathVisitor.withLongCounters();
        final Path dummyPath = new MockFile("any-file.txt").toPath();
        final BasicFileAttributes nullAttributes = null;

        // Act: Call the method under test with null attributes.
        // This is expected to throw the NullPointerException.
        visitor.visitFile(dummyPath, nullAttributes);

        // Assert: The test framework asserts that a NullPointerException was thrown,
        // as specified by the @Test(expected=...) annotation.
    }
}