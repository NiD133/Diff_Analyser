package org.jsoup.parser;

import org.junit.Test;
import static org.junit.Assert.assertEquals;

/**
 * This class contains tests for the TokenQueue class.
 * The original test was auto-generated by EvoSuite. This version has been
 * refactored for better understandability.
 */
public class TokenQueue_ESTestTest68 extends TokenQueue_ESTest_scaffolding {

    /**
     * Verifies that chompBalanced returns an empty string when called on an empty queue.
     */
    @Test
    public void chompBalancedOnEmptyQueueReturnsEmptyString() {
        // Arrange: Create an empty token queue.
        // The original test created a queue with content and then consumed it all.
        // Starting with an empty queue is more direct and clearer.
        TokenQueue queue = new TokenQueue("");

        // Act: Attempt to consume a balanced sequence from the empty queue.
        // The open and close characters are arbitrary as the queue is empty.
        String result = queue.chompBalanced('(', ')');

        // Assert: The result should be an empty string.
        assertEquals("", result);
    }
}