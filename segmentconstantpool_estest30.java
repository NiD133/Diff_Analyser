package org.apache.commons.compress.harmony.unpack200;

import org.junit.Test;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.fail;

// The original test was generated by EvoSuite. This version has been refactored
// for better human readability and maintainability.
public class SegmentConstantPool_ESTestTest30 extends SegmentConstantPool_ESTest_scaffolding {

    /**
     * Tests that the internal `regexMatches` method throws an `Error` when it
     * encounters a regular expression pattern it is not designed to handle.
     *
     * The method is a highly specialized, non-standard regex implementation
     * that only supports a small, hard-coded set of patterns. This test
     * verifies the documented failure-case for unsupported patterns.
     */
    @Test
    public void regexMatchesShouldThrowErrorForUnsupportedPattern() {
        final String unsupportedPattern = "gml`F";
        final String anyCompareString = "6x,WAe3"; // The content of this string is irrelevant for this test.

        try {
            SegmentConstantPool.regexMatches(unsupportedPattern, anyCompareString);
            fail("Expected an Error to be thrown for an unsupported regex pattern, but none was thrown.");
        } catch (final Error e) {
            final String expectedMessage = "regex trying to match a pattern I don't know: " + unsupportedPattern;
            assertEquals("The error message should identify the unsupported pattern.", expectedMessage, e.getMessage());
        }
    }
}