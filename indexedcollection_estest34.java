package org.apache.commons.collections4.collection;

import org.junit.Test;

import java.util.Collection;
import java.util.LinkedList;

/**
 * Tests for the exception handling in {@link IndexedCollection}.
 * This is a refactoring of a test originally generated by a test generation tool.
 */
public class IndexedCollection_ESTestTest34 extends IndexedCollection_ESTest_scaffolding {

    /**
     * Tests that containsAll() throws a NullPointerException when given a null collection.
     * The contract for Collection.containsAll() specifies that it should throw a NullPointerException
     * if the specified collection is null.
     */
    @Test(expected = NullPointerException.class)
    public void containsAllShouldThrowNullPointerExceptionForNullInput() {
        // Arrange
        // An empty backing collection is sufficient for this test.
        final Collection<String> backingCollection = new LinkedList<>();
        final IndexedCollection<String, String> indexedCollection =
                IndexedCollection.uniqueIndexedCollection(backingCollection, null);

        // Act
        // This call is expected to throw a NullPointerException, which is
        // asserted by the @Test(expected=...) annotation.
        indexedCollection.containsAll(null);
    }
}