package org.apache.commons.collections4.bag;

import org.apache.commons.collections4.Bag;
import org.junit.Test;

import static org.junit.Assert.assertFalse;

/**
 * This class contains tests for CollectionBag, focusing on the remove method.
 * The original test was auto-generated by EvoSuite.
 */
public class CollectionBag_ESTestTest45 extends CollectionBag_ESTest_scaffolding {

    /**
     * Tests that calling remove() with an object of an incompatible type on an
     * empty bag returns false, as the bag is not modified.
     */
    @Test
    public void testRemoveIncompatibleTypeFromEmptyBagReturnsFalse() {
        // Arrange: Create an empty CollectionBag that holds Integers.
        final Bag<Integer> emptyBag = new CollectionBag<>(new HashBag<>());
        final Object objectOfIncompatibleType = "a string";

        // Act: Attempt to remove the object from the bag.
        final boolean wasRemoved = emptyBag.remove(objectOfIncompatibleType);

        // Assert: The remove operation should return false because the bag did not contain
        // the object and was therefore not changed.
        assertFalse("Removing an object of an incompatible type from an empty bag should return false.", wasRemoved);
    }
}