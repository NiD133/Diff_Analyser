package org.jsoup.nodes;

import org.junit.Test;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.fail;

/**
 * This class contains tests for the {@link Attribute} constructor.
 * Note: This is an improved version of a test originally generated by EvoSuite.
 */
public class Attribute_ESTestTest29 extends Attribute_ESTest_scaffolding {

    /**
     * Verifies that the Attribute constructor throws an IllegalArgumentException
     * when the provided key is null.
     */
    @Test
    public void constructorShouldThrowIllegalArgumentExceptionForNullKey() {
        // The Attribute constructor must not accept a null key.
        // This test verifies that an IllegalArgumentException is thrown, as specified by the contract.
        
        try {
            // Act: Attempt to create an Attribute with a null key.
            new Attribute(null, "any-value");
            fail("Expected an IllegalArgumentException to be thrown for a null attribute key, but it was not.");
        } catch (IllegalArgumentException e) {
            // Assert: Verify the exception has the expected message from the validation logic.
            assertEquals("Object must not be null", e.getMessage());
        }
    }
}