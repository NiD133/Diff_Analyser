package org.apache.commons.lang3;

import static org.junit.Assert.assertEquals;
import org.junit.Test;

/**
 * Tests for {@link CharSetUtils}.
 * 
 * The original test class name "CharSetUtils_ESTestTest19" was uninformative and
 * suggested it was generated by a tool. A more standard name is used here for clarity.
 */
public class CharSetUtilsTest {

    /**
     * Tests that CharSetUtils.count() returns 0 when the input string is empty.
     *
     * <p>This holds true even when the provided character set contains null elements.
     * The test verifies the behavior documented in the Javadoc:
     * {@code CharSetUtils.count("", *) = 0}.</p>
     */
    @Test
    public void countShouldReturnZeroForEmptyString() {
        // Arrange: Define the input string and the character set.
        // The original test used an arbitrary `new String[13]`. An array with a single
        // null element is sufficient to test this edge case more concisely.
        final String[] charSetWithNulls = new String[1]; // Creates an array containing one null
        final String stringToSearch = "";
        final int expectedCount = 0;

        // Act: Call the method under test.
        final int actualCount = CharSetUtils.count(stringToSearch, charSetWithNulls);

        // Assert: Verify that the result is 0.
        assertEquals("Count of characters in an empty string should always be 0",
                expectedCount, actualCount);
    }
}