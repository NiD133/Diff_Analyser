package com.google.gson.internal;

import org.junit.Test;
import static org.junit.Assert.*;

/**
 * This test class contains an improved version of a test case for LinkedTreeMap.
 * The original was auto-generated by EvoSuite, and this version has been
 * refactored for better human comprehension and maintainability.
 */
public class LinkedTreeMap_ESTestTest63 extends LinkedTreeMap_ESTest_scaffolding {

    /**
     * Verifies that calling `put()` with a null value throws a NullPointerException
     * when the map has been configured to disallow null values.
     */
    @Test(timeout = 4000)
    public void put_shouldThrowNullPointerException_whenValueIsNullAndNullsAreDisallowed() {
        // Arrange: Create a LinkedTreeMap that does not allow null values.
        // The original test used a complex generic type, which is simplified here for clarity.
        LinkedTreeMap<Integer, String> map = new LinkedTreeMap<>(false);
        Integer key = 816;

        // Act & Assert: Verify that attempting to put a null value throws the expected exception.
        try {
            map.put(key, null);
            fail("Expected a NullPointerException to be thrown, but it was not.");
        } catch (NullPointerException e) {
            // The source code specifies this exact message, so we assert it for correctness.
            assertEquals("value == null", e.getMessage());
        }
    }
}